/**
 * Common tool types for the Quill JS runtime.
 *
 * Mirrors `quill.tools.types` from the Python backend.
 */

import type { StructuredToolInterface } from "@langchain/core/tools";

export type QuillTool = StructuredToolInterface;
