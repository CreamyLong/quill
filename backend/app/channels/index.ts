/**
 * IM Channel integration for Quill.
 *
 * Provides a pluggable channel system that connects external messaging platforms
 * (Feishu/Lark, Slack, Telegram) to the Quill agent via the ChannelManager,
 * which uses `langgraph-sdk` to communicate with Gateway's LangGraph-compatible API.
 */

// This package is intentionally a placeholder; concrete channel implementations
// will be re-exported here once they are migrated to TypeScript.
